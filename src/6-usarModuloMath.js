/*
    Describo mo importar y usar el modulo math:

        - Este modulo ha sido creado por mi y contiene funciones básicas de matemáticas
        - Importo el modulo math y lo almaceno en la constante math
        - El nombre de la constante que lo almacena suele tener el mismo nombre que el modulo
        - Uso las funciones que contiene el módulo y muestro los resultados por consola
        
*/
const math = require('../modules/math.js');
console.log(math.add(1, 2))
console.log(math.subtract(1, 2))
console.log(math.multiply(1, 2))
console.log(math.divide(1, 2))

